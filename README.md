# **Задача для веб- и fullstack - разработчиков от Digital агентства Победа**

## **Список пользователей**

Список пользователей – это простое веб-приложение для отображения пользователей и информации о каждом, а также для добавления новых пользователей.

Бэкенд разработан на **Flask**, а фронтенд – на **JavaScript**. Взаимодействие между частями осуществляется через **Fetch API**. Используемая база данных **SQLite**.

---

### **Требования**

Убедитесь, что в вашей системе установлены Python и Node js:

- [Python version 3.14.0+](https://www.python.org/downloads/release/python-3140/)
- [Node version 24.11.0+](https://nodejs.org/en/download/)

---

### **Структура проекта**

- [Описание задания](project_info)
- Структура пользовательского интерфейса — frontend:
  
  📁 `frontend/` – корневая папка фронтенда, содержащая все необходимые файлы для клиентской части приложения  

  &nbsp;&nbsp;&nbsp;&nbsp;📄 `index.html` – главная страница приложения, на которой отображается пользовательский интерфейс  
  &nbsp;&nbsp;&nbsp;&nbsp;📄 `script.js` – файл, содержащий все JavaScript-скрипты, которые управляют взаимодействием на странице, обрабатывают запросы к API и динамически обновляют контент  
  &nbsp;&nbsp;&nbsp;&nbsp;📄 `styles.css` – файл со стилями, который отвечает за внешний вид приложения, включая цвета, шрифты и оформление элементов

- Структура серверной части приложения — backend:

  📁 `backend/` – корневая папка бэкенда, содержащая все компоненты серверной части приложения  
  &nbsp;&nbsp;&nbsp;&nbsp;📁 **`instance/`** – папка для хранения файлов базы данных  
  &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;📄 **`database.db`** – файл базы данных, хранящий данные приложения

  &nbsp;&nbsp;&nbsp;&nbsp;📄 `app.py` – основной файл приложения, содержащий код сервера и маршруты для обработки HTTP-запросов  
  &nbsp;&nbsp;&nbsp;&nbsp;📄 `config.py` – файл с настройками приложения  
  &nbsp;&nbsp;&nbsp;&nbsp;📄 `models.py` – файл, определяющий модель базы данных  
  &nbsp;&nbsp;&nbsp;&nbsp;📄 `requirements.txt` – файл, содержащий список зависимостей проекта, необходимых для его работы

---

### **Локальный запуск приложения**

1. Запускаем терминал в директории, где планируется разместить папку с файлами проекта
2. Клонируем репозиторий:\
   `git clone https://github.com/AntistesEM/Digital_agency_Pobeda-taskTest.git`
3. Открываем папку `Digital_agency_Pobeda-taskTest` в любой IDE.
4. Запускаем приложение:

      -- запускаем встроенный терминал

      -- переходим в папку `backend`:

      ```bash
      cd backend
      ```

      -- создаём виртуальное окружение:

      ```bash
      python -m venv venv
      ```

      -- активируем его:

      ```bash
      .\venv\Scripts\activate
      ```

      -- устанавливаем зависимости:

      ```bash
      pip install -r requirements.txt
      ```

      -- запускаем сервер (при этом база данных будет создана автоматически):

      ```bash
      python app.py
      ```

    После этого по ссылке [127.0.0.1:5000](http://127.0.0.1:5000/) будет доступна главная страница приложения.

    Эндпоинты:

   - [127.0.0.1:5000](http://127.0.0.1:5000/users/) будет доступен список всех пользователей.
   - [127.0.0.1:5000](http://127.0.0.1:5000/users/1/) будет доступна информация о пользователе с id = 1.

---

### **Используемые технологии**

#### Backend

- **Язык программирования**: Python
- **Фреймворк**: Flask
- **API**: RESTful API (с использованием Flask)
- **База данных**: SQLite
- **Шаблонизатор**: Jinja2 (входит в Flask для рендеринга HTML-шаблонов)
- **Обработка ошибок**: SQLAlchemy для управления транзакциями и обработки ошибок, таких как IntegrityError и SQLAlchemyError
- **Сервер приложений**: Flask Development Server

#### Frontend

- **Язык программирования**: JavaScript
- **Работа с API**: Fetch API
- **Обработка событий**: DOM Events
- **Базовая вёрстка**: HTML/CSS
- **Стилизация**: Bootstrap

---

![Анимация работы приложения](imageWork/Анимация_работы.gif)
